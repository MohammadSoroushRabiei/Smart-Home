نقشه راه پروژه کارشناسی — سیستم خانه هوشمند مبتنی بر IoT و کنترل دسترسی هوشمند AI Edge
استاد: دکتر امیر خورسندی | دانشجو: محمدسروش ربیعی
آخرین به‌روزرسانی: ۲۶ شهریور ۱۴۰۵

۱. تاریخ‌ها و بودجه
| رویداد|تاریخ شمسی|میلادی|
| ---|---|---|
| امروز|۲۶ شهریور|۱۷ سپتامبر ۲۰۲۶|
| ارسال گزارش|۲۸ شهریور، ساعت ۲۳:۵۹|۱۹ سپتامبر ۲۰۲۶|
| دفاع|۶ مهر|۲۸ سپتامبر ۲۰۲۶|
تا ارسال گزارش: ۳ روز (۲۶، ۲۷، ۲۸) × ۵ ساعت ≈ ۱۵ ساعت
تا دفاع: ۱۱ روز × ۵ ساعت = ۵۵ ساعت، منهای ~۱۰-۱۲ ساعت پاورپوینت/فیلم دمو/تمرین دفاع → ~۴۳-۴۵ ساعت مهندسی واقعی

۲. کارهای باقی‌مانده تا تحویل گزارش (۲۸ شهریور، ۲۳:۵۹)
✅ پیاده‌سازی کامل صفحه تنظیمات وای‌فای (اسکن، اتصال، Forget، منوی Long-press، UX بهبودیافته) - انجام شد در ۲۵ شهریور
[ ] نوشتن گزارش پروژه
[ ] ساخت داشبورد زیبا و مرتب در Home Assistant
[ ] بهبود UI صفحه‌ی اصلی LCD: نمایش دما، رطوبت و فشار به‌صورت جداگانه و زیبا (به‌جای یک لیبل واحد فعلی)
[ ] کالیبراسیون threshold تشخیص چهره (فعلاً هاردکد `0.70f`)

۳. کارهای باقی‌مانده بعد از تحویل گزارش
سخت‌افزار / سیم‌کشی
[ ] آماده‌سازی و اتصال مدار قفل سلونوئیدی واقعی (جایگزینی LED تست unlock با رله + قفل واقعی)
[ ] آماده‌سازی و اتصال مدار لامپ واقعی (جایگزینی LED تست light با رله + لامپ واقعی)
[ ] ساخت جعبه و محکم‌سازی اتصالات سیم‌کشی

نرم‌افزار / باگ / فیچرهای جدید
[ ] پیاده‌سازی صفحه تنظیمات MQTT/HA (دریافت IP سرور از کاربر، ذخیره در NVS)
[ ] منطق اتصال خودکار MQTT در بوت (با Retry محدود و توقف در صورت شکست مکرر)
[ ] دکمه اتصال دستی MQTT در صفحه اصلی (تپ برای اتصال/تلاش مجدد، هولد برای باز کردن تنظیمات MQTT)
[ ] رفع باگ قفل‌شدن تاچ روی باس I2C (هم بررسی ریشه‌ی سخت‌افزاری، هم نرم‌افزاری)
[ ] Watchdog و مدیریت خطای بهتر در `receive_jpeg_body` (تست/استحکام حداقلی)
[ ] اختیاری: چند-نمونه‌ای (multi-sample) در Enroll برای بهبود دقت

Home Assistant / امنیت (در انتظار شاخه‌ی جداگانه‌ی مدیریت Wi-Fi/MQTT/credentials)
[ ] Automation در HA
[ ] امن‌سازی Mosquitto (فعلاً `allow_anonymous true`) و دسترسی از بیرون شبکه

ریسک‌های امنیتی پذیرفته‌شده (جزئیات کامل در بخش ۴)
[ ] TODO اختیاری: اسکن ID-به-ID برای پاک‌سازی رکوردهای orphan در دیتابیس چهره
[ ] TODO اختیاری: محافظت brute-force روی توکن Enroll

الزام دائمی (باید همیشه رعایت شود)
[ ] چراغ/قفل/سنسور هرگز نباید به دسترسی Home Assistant وابسته‌ی سخت شوند
[ ] فقدان liveness detection — محدودیت شناخته‌شده، صادقانه در دفاع اعلام شود

آماده‌سازی دفاع و انتشار
[ ] آماده‌سازی پاورپوینت و فیلم دمو برای دفاع
[ ] مستندسازی کامل و آماده‌سازی نمایش پروژه برای انتشار در گیت‌هاب به‌عنوان رزومه

۴. ⚠️ ریسک‌های امنیتی شناخته‌شده و آگاهانه پذیرفته‌شده
این پروژه یافته‌ی امنیتی «بحرانی و حل‌نشده» ندارد، ولی دو ریسک کوچک‌تر و کنترل‌شده به‌صورت آگاهانه برای صرفه‌جویی در وقت پذیرفته شدند. هر دو باید صادقانه در دفاع قابل توضیح باشند:
۱. رکوردهای «یتیم» (orphan) در دیتابیس تشخیص چهره
اگر تعداد چهره‌های واقعی در دیتابیس کتابخانه (`get_num_feats()`) بیشتر از تعداد رکوردهای نام‌دار در `face_db` باشد، آن چهره‌های اضافی در سیستم قابل‌مشاهده نیستند ولی همچنان می‌توانند قفل را باز کنند.
چرا حل نشد: تنها راه امن برای پاک کردن فقط رکوردهای یتیم نیاز به اسکن ID به ID با فراخوانی `delete_feat()` روی IDهای نامعلوم دارد که رفتارشان بدون دیدن سورس داخلی کتابخانه تضمین‌شده نیست.
راهکار پذیرفته‌شده: قبل از ارائه/دفاع، یک‌بار به‌صورت دستی کل NVS/SPIFFS پاک می‌شود (`idf.py erase-flash`) و فقط چهره‌های معتبر دوباره enroll می‌شوند.
TODO برای فرصت بعدی: پیاده‌سازی اسکن ID-به-ID با تست دقیق روی رفتار `delete_feat()` روی ID نامعتبر.
۲. بدون محافظت در برابر حدس زدن (brute-force) توکن Enroll
توکن ۶ رقمی (۱ میلیون حالت، ۳ دقیقه اعتبار) هیچ محدودیتی روی تعداد تلاش‌های ناموفق ندارد. تصمیم گرفته شد این محافظت اضافه نشود.
چرا پذیرفته شد: ریسک عملی محدود است - مهاجم باید هم روی شبکه‌ی محلی خانه باشد هم در بازه‌ی ۳ دقیقه‌ای فعال، و حتی موفق، فقط به enrollment دسترسی می‌دهد نه unlock مستقیم.
TODO برای فرصت بعدی: افزودن شمارنده‌ی تلاش ناموفق در صورت وجود وقت اضافه.

۵. فازبندی زمانی
| بازه|کار|
| ---|---|
| ۲۶ تا ۲۸ شهریور|نوشتن گزارش پروژه؛ ساخت داشبورد HA؛ بهبود UI LCD (دما/رطوبت/فشار جداگانه)؛ کالیبراسیون threshold در صورت زمان اضافه؛ شروع پیاده‌سازی تنظیمات MQTT و ذخیره IP در NVS|
| ۲۸ شهریور، ۲۳:۵۹|ارسال گزارش|
| ۲۹ شهریور تا ۳ مهر|تکمیل منطق اتصال MQTT (Retry، دکمه UI تپ/هولد)؛ Automation در HA؛ Watchdog/استحکام حداقلی؛ مدار واقعی قفل سلونوئیدی و لامپ؛ ساخت جعبه و سیم‌کشی؛ رفع باگ I2C تاچ؛ مستندسازی گیت‌هاب؛ امن‌سازی Mosquitto (بعد از تکمیل شاخه‌ی جداگانه)|
| ۴ تا ۵ مهر|آماده‌سازی پاورپوینت و فیلم دمو؛ تمرین دفاع؛ اگر وقت ماند: TODOهای ریسک‌های امنیتی یا Stretch Goals|
| ۶ مهر|دفاع|

۶. Stretch Goals
انتقال HA/Mosquitto به لپ‌تاپ قدیمی (سرور دائمی) — Docker Desktop روی لپ‌تاپ فعلی برای MVP/دمو کافی است — ۳-۵ ساعت
حل ریشه‌ای orphan-record scan (بخش ۴، TODO ۱) — ۲-۴ ساعت
محافظت brute-force روی توکن enroll (بخش ۴، TODO ۲) — ۱-۲ ساعت
افزودن WebAuthn به مکانیزم توکن موجود — ۱۰-۱۴ ساعت
اپ Flutter اختصاصی — اولویت پایین
PCB (Altium) — فقط طراحی/رندر
UI/UX با SquareLine Studio
پنل کنترل سریع
Liveness detection
iframe کارت capture در HA dashboard
BLE/iBeacon | NFC | دوربین OV2640 روی برد | OTA

۷. جمع‌بندی ساعت‌ها
| گروه|ساعت|
| ---|---|
| تا تحویل گزارش (بخش ۲)|~۸-۱۲|
| بعد از تحویل گزارش - نرم‌افزار/HA/باگ/MQTT (بخش ۳)|~۱۲-۱۸|
| بعد از تحویل گزارش - سخت‌افزار/سیم‌کشی/دفاع/مستندسازی (بخش ۳)|~۱۰-۱۵|
| در انتظار شاخه‌ی جداگانه (امن‌سازی Mosquitto + دسترسی بیرونی)|۴-۷ (بعد از merge آن شاخه دوباره برآورد می‌شود)|
| Stretch|+۱۵-۲۳|

۸. نکات فنی TLS/HTTPS 🔐
`config.servercert`/`config.servercert_len` نه `cacert_pem`
`EMBED_TXTFILES` symbol naming بر اساس مسیر نسبی فایل
`CONFIG_MBEDTLS_EXTERNAL_MEM_ALLOC=y` برای کمبود DRAM حین handshake
`max_uri_handlers` پیش‌فرض ۸ - با هر هندلر جدید هماهنگ نگه دار

۹. نکات فنی Home Assistant / Docker / MQTT 🏠
Docker Desktop روی ویندوز پورت‌ها را روی IP واقعی هم expose می‌کند
`ha-config/`, `mosquitto/data/`, `mosquitto/log/` در `.gitignore`
Mosquitto بدون `mosquitto.conf` هیچ پورتی listen نمی‌کند
MQTT integration در HA به broker با اسم سرویس Docker وصل می‌شود، نه IP
`esp-mqtt` کامپوننت built-in است؛ فقط نیاز به `PRIV_REQUIRES "mqtt"`
معماری قفل دو-لایه: `lock.c` گنگ (فقط رله + fail-safe مستقل)؛ `app_state.c` تنها نقطه‌ی ورودی رسمی
`binary_sensor` به‌جای `lock` کامل برای وضعیت قفل (تصمیم امنیتی تا امن شدن Mosquitto)
`event` entity به‌جای `binary_sensor` برای رویدادهای دسترسی

۱۰. نکات فنی باس I2C مشترک 🔌
مشکل: race condition بین `sensor_task` و `touch_poll_task` روی باس مشترک، بدون timeout داخلی در درایور GT911
راه‌حل فعلی: mutex اپلیکیشنی (`i2c_bus_lock`/`i2c_bus_unlock`) دور هر تراکنش منطقی
قاعده: هر تابعی که lock می‌کند، باید در همه‌ی مسیرهای خروج unlock کند
⚠️ هنوز گزارش قفل‌شدن تاچ رخ می‌دهد - نیاز به بررسی هم سخت‌افزاری (نویز/سیم‌کشی باس) هم نرم‌افزاری (بخش ۳)

۱۱. نکات فنی enrollment و مدیریت چهره 🔐
`HumanFaceRecognizer::enroll()` هیچ ID برنمی‌گرداند (فقط `esp_err_t`) — برای گرفتن ID واقعی، بلافاصله بعد از enroll باید همان تصویر یک‌بار `recognize()` شود
کتابخانه هیچ متد public ای برای لیست کردن چهره‌های ثبت‌شده ندارد — نیاز به لایه‌ی متادیتای مستقل (`face_db.c/.h` روی NVS)
اسم‌گذاری چهره باید از دستگاهی با کیبورد کامل بیاید (گوشی کاربر در `/enroll`)، نه از کیبورد عددی LCD
اعتبارسنجی توکن باید هم روی GET هم روی POST انجام شود

۱۲. نکات فنی مدیریت اتصال MQTT 🔌

- ذخیره‌سازی IP سرور MQTT در NVS (مشابه الگوی `wifi_config`) برای پایداری بین ری‌بوت‌ها.
- دکمه UI در صفحه اصلی: `LV_EVENT_CLICKED` برای اتصال دستی (یا تلاش مجدد)، `LV_EVENT_LONG_PRESSED` برای باز کردن صفحه تنظیمات MQTT.
- منطق Retry: استفاده از یک شمارنده در `mqtt_manager` که بعد از N بار شکست متوالی (مثلاً ۵ بار)، تسک اتصال را متوقف می‌کند تا مصرف باتری/CPU بیهوده نباشد. ریست شدن شمارنده فقط با اتصال موفق یا درخواست دستی کاربر انجام می‌شود.
